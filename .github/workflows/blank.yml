name: Test Action

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Hello World
        run: echo "Hello, World!"
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'     
