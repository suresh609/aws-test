name: Test Action

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Hello World
        run: echo "Hello, World!"
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'
      
      - name: Send CI data to Datadog
        uses: datadog-ci/ci-visibility-action@v2
        with:
          datadog-api-key: "20554c3e044b04aa7351f90c7fbafcca"
          service: github-ci-pipeline
          environment: production
          site: datadoghq.eu
